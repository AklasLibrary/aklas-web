@{
    if (Context.Request.Path.StartsWithSegments("/Admin", StringComparison.OrdinalIgnoreCase))
        Layout = "_LayoutAdmin";
    else if (Context.Request.Path.StartsWithSegments("/Librarian", StringComparison.OrdinalIgnoreCase))
        Layout = "_LayoutLibrarian";
    else 
        Layout = "_Layout";
}
